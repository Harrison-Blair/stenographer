# CLAUDE.md

Guidance for Claude Code in this repository lives in `AGENTS.md`, which is the
single canonical agent-guidance file (shared with every other AI tool). It is
imported below; do not duplicate content here — edit `AGENTS.md` instead.

@AGENTS.md
